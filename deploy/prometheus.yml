global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'os_autofix_engine'
    static_configs:
      - targets: ['host.docker.internal:9100', '127.0.0.1:9100']
        labels:
          environment: 'production'
          app: 'os-autofix-engine'
